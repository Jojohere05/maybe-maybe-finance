name: FinGuard Compliance Scan

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  scan:
    uses: Jojohere05/PBL-2/.github/workflows/finguard-api-local.yml@main
    with:
      repo: ${{ github.repository }}
